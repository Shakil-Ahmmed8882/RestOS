const categories = ["web development", "Design", "other"];
export const categoryOptions = categories.map((item) => ({
  value: item,
  label: item,
}));

const tags = ["Recipes", "Events", "News"];
export const tagOptions = tags.map((item) => ({
  value: item,
  label: item,
}));
